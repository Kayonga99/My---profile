/*eslint-disable */
(function ($) {
  $.fn.onScreen = function (options) {
    const params = $.extend({
      container: window,
      direction: 'vertical',
      toggleClass: null,
      doIn: null,
      doOut: null,
      tolerance: 0,
      throttle: null,
      lazyAttr: null,
      lazyPlaceholder: 'data:image/gif;base64,',
      debug: false,
    }, options);
    return this.each(function () {
      let isOnScreen = false;
      let scrollTop; let scrollLeft;
      const $el = $(this); let $container;
      let containerHeight;
      let containerWidth;
      let containerBottom;
      let containerRight;
      let elHeight;
      let elWidth;
      let elTop;
      let elLeft;
      const containerIsWindow = $.isWindow(params.container);

      function verticalIn() {
        if (containerIsWindow) {
          return elTop < containerBottom - params.tolerance && scrollTop < (elTop + elHeight) - params.tolerance;
        }
        return elTop < containerHeight - params.tolerance && elTop > (-elHeight) + params.tolerance;
      }
      function verticalOut() {
        if (containerIsWindow) {
          return elTop + (elHeight - params.tolerance) < scrollTop || elTop > containerBottom - params.tolerance;
        } return elTop > containerHeight - params.tolerance || -elHeight + params.tolerance > elTop;
      }
      function horizontalIn() {
        if (containerIsWindow) {
          return elLeft < containerRight - params.tolerance && scrollLeft < (elLeft + elWidth) - params.tolerance;
        }
        return elLeft < containerWidth - params.tolerance && elLeft > (-elWidth) + params.tolerance;
      }
      function horizontalOut() {
        if (containerIsWindow) {
          return elLeft + (elWidth - params.tolerance) < scrollLeft || elLeft > containerRight - params.tolerance;
        }
        return elLeft > containerWidth - params.tolerance || -elWidth + params.tolerance > elLeft;
      }
      function directionIn() {
        if (isOnScreen) {
          return false;
        }
        if (params.direction === 'horizontal') { return horizontalIn(); } return verticalIn();
      }

      function directionOut() {
        if (!isOnScreen) {
          return false;
        } if (params.direction === 'horizontal') {
          return horizontalOut();
        } return verticalOut();
      }
      function throttle(fn, timeout, ctx) {
        let timer; let args; let needInvoke;
        return function () {
          args = arguments; needInvoke = true; ctx = ctx || this;
          if (!timer) {
            (function () {
              if (needInvoke) {
                fn.apply(ctx, args);
                needInvoke = false; timer = setTimeout(arguments.callee, timeout);
              } else { timer = null; }
            }());
          }
        };
      }

      let checkPos = function () {
        if (!containerIsWindow && $(params.container).css('position') === 'static') {
          $(params.container).css('position', 'relative');
        }
        $container = $(params.container);
        containerHeight = $container.height();
        containerWidth = $container.width();
        containerBottom = $container.scrollTop() + containerHeight;
        containerRight = $container.scrollLeft() + containerWidth; elHeight = $el.outerHeight(true);
        elWidth = $el.outerWidth(true);
        if (containerIsWindow) {
          const offset = $el.offset();

          elTop = offset.top; elLeft = offset.left;
        } else {
          const position = $el.position();
          elTop = position.top; elLeft = position.left;
        }
        scrollTop = $container.scrollTop();
        scrollLeft = $container.scrollLeft();
        if (params.debug) {
          console.log(`Container: ${params.container}\n` + `Width: ${containerHeight}\n` + `Height: ${containerWidth}\n` + `Bottom: ${containerBottom}\n` + `Right: ${containerRight}`);
          console.log(`Matched element: ${$el.attr('class') !== undefined ? `${$el.prop('tagName').toLowerCase()}.${$el.attr('class')}` : $el.prop('tagName').toLowerCase()}\n` + `Left: ${elLeft}\n` + `Top: ${elTop}\n` + `Width: ${elWidth}\n` + `Height: ${elHeight}`);
        }
        if (directionIn()) {
          if (params.toggleClass) {
            $el.addClass(params.toggleClass);
          }
          if ($.isFunction(params.doIn)) {
            params.doIn.call($el[0]);
          }
          if (params.lazyAttr && $el.prop('tagName') === 'IMG') {
            const lazyImg = $el.attr(params.lazyAttr); if (lazyImg !== $el.prop('src')) {
              $el.css({
                background: `url(${params.lazyPlaceholder}) 50% 50% no-repeat`,
                minHeight: '5px',
                minWidth: '16px',
              }); $el.prop('src', lazyImg).load(function () {
                $(this).css({ background: 'none' });
              });
            }
          }
          isOnScreen = true;
        } else if (directionOut()) {
          if (params.toggleClass) {
            $el.removeClass(params.toggleClass);
          } if ($.isFunction(params.doOut)) {
            params.doOut.call($el[0]);
          }isOnScreen = false;
        }
      };
      if (window.location.hash) {
        throttle(checkPos, 50);
      } else {
        checkPos();
      } if (params.throttle) {
        checkPos = throttle(checkPos, params.throttle);
      }$(params.container).on('scroll resize', checkPos); if (!containerIsWindow) {
        $(window).on('resize', checkPos);
      } if (typeof module === 'object' && module && typeof module.exports === 'object') {
        module.exports = jQuery;
      } else if (typeof define === 'function' && define.amd) {
        define('jquery-onscreen', [], () => jQuery);
      }
    });
  };
}(jQuery));
const is = {
  ie: navigator.appName == 'Microsoft Internet Explorer',
  java: navigator.javaEnabled(),
  ns: navigator.appName == 'Netscape',
  ua: navigator.userAgent.toLowerCase(),
  version: parseFloat(navigator.appVersion.substr(21)) || parseFloat(navigator.appVersion),
  win: navigator.platform == 'Win32',
};
is.mac = is.ua.indexOf('mac') >= 0; if (is.ua.indexOf('opera') >= 0) {
  is.ie = is.ns = false; is.opera = true;
} if (is.ua.indexOf('gecko') >= 0) {
  is.ie = is.ns = false; is.gecko = true;
}
