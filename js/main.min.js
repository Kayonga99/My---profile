/*eslint-disable */
!(function (t, e) { const n = function (t, e, n) { let i; return function () { function a() { n || t.apply(r, o), i = null; } var r = this; var o = arguments; i ? clearTimeout(i) : n && t.apply(r, o), i = setTimeout(a, e || 100); }; }; jQuery.fn[e] = function (t) { return t ? this.bind('resize', n(t)) : this.trigger(e); }; }(jQuery, 'smartresize')), $(() => { function t() { const t = jQuery('#header .middle'); const e = jQuery('#header').actual('height') / 2 - jQuery('#header .middle').actual('height') / 2 - 20; t.css('padding-top', `${e}px`), t.show(); } const e = function () { const t = $(window).height(); $('#overlay-1').height(t); }; e(), $('a[href*=#]:not([href=#])').click(function () { if (location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '') && location.hostname == this.hostname) { let t = $(this.hash); if (t = t.length ? t : $(`[name=${this.hash.slice(1)}]`), t.length) return $('html,body').animate({ scrollTop: t.offset().top }, 1e3), !1; } }); const n = $('.bottom > a'); var i = function () { n.animate({ top: 5 }, 400, 'linear', () => { n.animate({ top: -5 }, 400, 'linear', () => { i(); }); }); }; i(), t(), jQuery(window).smartresize(() => { e(), t(); }); }), $(() => {
  const t = $('.isotope').isotope({
    itemSelector: '.element-item',
    layoutMode: 'fitRows',
    getSortData: {
      name: '.name', symbol: '.symbol', number: '.number parseInt', category: '[data-category]', weight(t) { const e = $(t).find('.weight').text(); return parseFloat(e.replace(/[\(\)]/g, '')); },
    },
  }); const e = { numberGreaterThan50() { const t = $(this).find('.number').text(); return parseInt(t, 10) > 50; }, ium() { const t = $(this).find('.name').text(); return t.match(/ium$/); } }; $('#filters').on('click', 'button', function () { let n = $(this).attr('data-filter'); n = e[n] || n, t.isotope({ filter: n }); }), $('#sorts').on('click', 'button', function () { const e = $(this).attr('data-sort-by'); t.isotope({ sortBy: e }); }), $('.button-group').each((t, e) => { const n = $(e); n.on('click', 'button', function () { n.find('.is-checked').removeClass('is-checked'), $(this).addClass('is-checked'); }); });
});