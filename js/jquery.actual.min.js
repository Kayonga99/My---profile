/*! Copyright 2012, Ben Lin (http://dreamerslab.com/)
 * Licensed under the MIT License (LICENSE.txt).
 *
 * Version: 1.0.13
 *
 * Requires: jQuery 1.2.3 ~ 1.8.2
 */
/*eslint-disable */
(function (a) {
  a.fn.extend({
    actual(b, l) {
      if (!this[b]) {
        throw `$.actual => The jQuery method "${b}" you called does not exist`;
      }
      const f = { absolute: false, clone: false, includeMargin: false };
      const i = a.extend(f, l); let e = this.eq(0); let h; let j;
      if (i.clone === true) {
        h = function () {
          const m = 'position: absolute !important; top: -1000 !important; ';
          e = e.clone().attr('style', m).appendTo('body');
        };
        j = function () {
          e.remove();
        };
      } else {
        const g = []; let d = ''; let c; h = function () {
          c = e.parents().andSelf().filter(':hidden'); d += 'visibility: hidden !important; display: block !important; ';
          if (i.absolute === true) {
            d += 'position: absolute !important; ';
          }

          c.each(function () {
            const m = a(this); g.push(m.attr('style')); m.attr('style', d);
          });
        };
        j = function () {
          c.each(function (m) {
            const o = a(this); const n = g[m]; if (n === undefined) {
              o.removeAttr('style');
            } else {
              o.attr('style', n);
            }
          });
        };
      }
      h();

      const k = /(outer)/g.test(b) ? e[b](i.includeMargin) : e[b]();

      j();
      return k;
    },
  });
}(jQuery));
